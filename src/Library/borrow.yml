info:
  name: borrow
  type: http

http:
  method: POST
  url: http://127.0.0.1:8000/borrowbook
  body:
    type: json
    data: |-
      {
        "userId" : 1,
        "bookId" : 38
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
